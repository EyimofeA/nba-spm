# New SPM workspace — SPM + RAPM sub-projects
# (zts/ has its own requirements.txt)

# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4

# SPM gradient-boosted models
xgboost>=2.0
lightgbm>=4.3
joblib>=1.3
PyYAML>=6.0

# Reporting (prettyprintrapm.py)
matplotlib>=3.8
xlsxwriter>=3.2

# RAPM — MySQL driver. Prefer mysqlclient for speed; fall back to PyMySQL.
mysqlclient>=2.2 ; platform_system != "Windows"
PyMySQL>=1.1

# Optional: Bayesian ridge path used by rapm_with_prior.run_bayes_model
# pymc>=5.10

# Notebooks
jupyterlab>=4.1
